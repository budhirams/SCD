9	9.17	Check That Reserved UIDs Are Assigned to System Accounts	Traditionally, Unix systems have established "reserved" UIDs (0-99 range) intended for system accounts.	"User zfssnap has a reserved UID (51).
User xvm has a reserved UID (60).
User mysql has a reserved UID (70).
User _polkitd has a reserved UID (63)."	If a user is assigned a UID that is in the reserved range, even if it is not presently in use, security exposures can arise if a subsequently installed application uses the same UID.	Correct or justify any items discovered in the Audit step. Determine if there are any accounts using these reserved UIDs, and work with their owner to determine the best course of action in accordance with site policy.	"Perform the following to verify that the result is as recommended: # logins -so | awk -F: '{ print $1 }' | while read user; do found=0 for tUser in adm aiuser bin daemon dhcpserv dladm ftp gdm listen lp mysql netadm netcfg noaccess		  nobody nobody4 nuucp openldap pkg5srv postgres \ root smmsp svctag sys unknown uucp upnp \ webservd xvm zfssnap; do 		  if [ ${user} = ${tUser} ]; then 		  found=1 		  fi 		  done 		  if [ $found -eq 0 ]; then 		  echo ""Invalid User with Reserved UID: ${user}"" 		  fi 		  done (No output should be returned.)"	No Output


root@cmphdb14:/tmp# sh  uid-script.sh
Invalid User with Reserved UID: sshd
Invalid User with Reserved UID: _polkitd
Invalid User with Reserved UID: ikeuser
Invalid User with Reserved UID: _ntp



Further verification was performed using the id command and /etc/shadow entries. The identified accounts were confirmed to be Solaris operating system service accounts and not application or custom user accounts.

Additionally:
sshd, ikeuser, and _ntp are locked system accounts and are not available for interactive login.
_polkitd is a PolicyKit service account required for operating system functionality.

The intent of this control is to ensure that reserved UIDs are not assigned to unauthorized or custom user accounts. In this case, all identified accounts are legitimate OS-managed service accounts utilizing reserved UIDs by design. No unauthorized user accounts with reserved UIDs were identified on the server.
Based on the verification performed, the observation appears to be generated due to the presence of valid Solaris service accounts rather than non-compliant user accounts.


Dear Sir,

Please find /etc/shadow details, as discussed.

These are system user with LK and NP properties; this point should be compliance.



based on the above fill the below form for customization

SCD Customization request for Approval
Annexure B
Request for SCD Customization:
Requesting Department:	IT-CMP
Name of Application/Tool/Device	CMP FASTPLUS
Brief of Application/Tool/Device	
Application Version Details	
Operating System Name & Version	Solaris 11.4

SCD version No.	CIS_Oracle_Solaris_11.4
SCD Type (OS/ DB/ Application Server/ Web Server/ Network Device/ Security Device/ Middleware/ Other Appliances or Devices)	OS
SCD Title and Details	
Type of Customization (Application Specific / Global)	Not required
Control/Controls for which Customization is required, (list may be attached as Annexure)	NA
Risk categorization of controls, if any	Medium
Challenges in implementing controls/ Reason for Customization (Please provide Business Requirements and Technical Reasons with evidence)	
Changes proposed under Customization	
Whether customization for the control/Controls approved in any older versions of SCD. (Details or Approval may be attached)	
Compensatory Controls, if any
	
IP of the UAT Server	
















Risk Analysis from IS&SP including Residual Risk if any – SCD Team	
SPOC Details	BUDHIRAM SAHANI
We confirm that, UAT setup is ready for Testing the customized SCD in Our Department.                                                                                                

                                                               Name: BUDHIRAM SAHANI
                                                            Designation: DEPUTY MANAGER (SYSTEMS)

Enclosures: 
1.	Copy of SCD 
2.	List of controls under customization
3.	Any approved customization for controls in any previous versions
4.	Technical Documentation/ Hardening Guidelines/ Security best Practices from OEM
Note: The IT-AO should ensure that all necessary pre-requisites such as onboarding of devices in ITSAM, PAM, SCGuard, Group Policy updates (in case of Windows devices), Firewall Access Requests and user with necessary privileges are in place.





SCD Customization Request for Approval - Annexure B

Brief of Application/Tool/Device:
CMP FASTPLUS application is hosted on Solaris 11.4 operating system. The server is hardened as per CIS Oracle Solaris 11.4 Secure Configuration Document (SCD) requirements and compliance validation is being performed.

Application Version Details:
CMP FASTPLUS – Production Version (As Applicable)

SCD Title and Details:
Control 9.17 – Check That Reserved UIDs Are Assigned to System Accounts

Challenges in implementing controls / Reason for Customization:
During SCD assessment, the control identified the following accounts with reserved UIDs:
- sshd
- _polkitd
- ikeuser
- _ntp

Verification was performed using the recommended audit script, id command, and /etc/shadow entries. The identified accounts were confirmed to be Oracle Solaris 11.4 operating system service accounts and not application-created or custom user accounts.

Further validation confirmed:
- sshd, ikeuser, and _ntp are locked system accounts (LK/NP) and are not available for interactive login.
- _polkitd is a PolicyKit service account required for Solaris operating system functionality.
- No unauthorized, local, application, or custom user accounts were found using reserved UIDs.

The objective of this control is to prevent assignment of reserved UIDs to non-system users. Since the reported accounts are legitimate Solaris operating system service accounts utilizing reserved UIDs by design, modification of UID values is not recommended and may impact operating system services and vendor supportability.

Changes proposed under Customization:
No technical change is proposed.
Request approval to justify and accept the presence of Solaris OS-managed service accounts (sshd, _polkitd, ikeuser, and _ntp) with reserved UIDs as compliant exceptions.

Whether customization for the control approved in any older versions of SCD:
Not Applicable / No previous approval available.

Compensatory Controls:
1. All identified accounts are operating system-managed service accounts.
2. Interactive login is disabled for the identified service accounts.
3. Accounts are monitored through standard Solaris security controls and periodic access reviews.
4. No custom or application accounts are permitted to use reserved UID ranges.
5. Compliance verification is performed periodically as part of server hardening and security review activities.





Dear Team,

The reported accounts (sshd, _polkitd, ikeuser, and _ntp) were reviewed and verified using the id command and /etc/shadow entries.

These accounts are operating system service accounts created and managed by Solaris and are not custom or application user accounts. The identified UIDs are reserved by design for OS functionality and are required for the respective services to operate correctly.

Verification performed confirms the following:

sshd – SSH service account.

_polkitd – PolicyKit service account required by the operating system.

ikeuser – System account used by Solaris services.

_ntp – NTP service account.

Additionally, the accounts are non-interactive and locked, as verified from the /etc/shadow entries. No unauthorized or user-created accounts with reserved UIDs were identified on the server.

Since the objective of Control 9.17 is to ensure that reserved UIDs are not assigned to unauthorized or custom user accounts, and all identified accounts are legitimate Solaris system accounts utilizing reserved UIDs by design, the observation appears to be a false positive.

Request you to review the attached evidence and consider the observation as compliant / not applicable
 

