Uncommon Network Protocols to be disabled	install lines present for dccp, sctp, rds, tipc.	"/etc/modprobe.d/CIS.conf:install rds /bin/true
/etc/modprobe.d/CIS.conf:install dccp /bin/true"	The Linux kernel modules support several network protocols that are not commonly used.	The Linux kernel modules support several network protocols that are not commonly used	If these protocols are not needed, it is recommended that they be disabled in the kernel.If the protocol is not required, it is recommended that the drivers not be installed to reduce the potential attack surface.	Edit or create a file in the /etc/modprobe.d/ directory ending in .conf and add the following line:   install dccp /bin/true install sctp /bin/true install rds /bin/true install install tipc /bin/true
Ensure default deny firewall policy	Loopback accept and anti-spoof rules present	Firewall default-zone : trusted,get-log-denied : off		A default deny all policy on connections ensures that any unconfigured network usage will be rejected.	With a default accept policy the firewall will accept any packet that is not configured to be denied. It is easier to white list acceptable usage than to black list unacceptable usage.	Run the following command to implement a default DROP policy:   # nft chain [table family] [table name] [chain name] { policy drop \; }  Example:# nft chain inet filter input { policy drop \; }  # nft chain inet filter forward { policy drop \; }  1. Firewall Management: Ensure host-based firewall is enabled  Firewall service (firewalld) should be installed, enabled, and running Unrestricted traffic exposure  dnf install -y firewalld systemctl enable firewalld systemctl start firewalld  2. Firewall Architecture  Ensure only one firewall utility is active  Avoid conflict between iptables, nftables, firewalld Rule conflicts, traffic bypass systemctl disable iptables --now systemctl disable ip6tables –now 3. Backend Configuration  Ensure correct firewall backend  firewalld should use nftables (RHEL 8/9)  Misconfiguration, rule failure firewall-cmd --info-backend 4. Network Segmentation  Set default zone  Default zone should be restricted (e.g., public/internal)  Open network exposure firewall-cmd --set-default-zone=public 5. Access Control  Allow only required ports/services Only necessary ports (e.g., SSH, application ports) should be open Unauthorized access firewall-cmd --permanent --add-service=ssh firewall-cmd --permanent --add-port=[port number]/tcp firewall-cmd –reload 6. Loopback Protection  Secure loopback traffic  Allow loopback and prevent spoofingLocal spoofing attack firewall-cmd --permanent --zone=trusted --add-interface=lo firewall-cmd –reload7. Rule Persistence  Ensure firewall rules persist after reboot  All rules must be permanent  Rules lost after reboot firewall-cmd --runtime-to-permanent 8. Logging & Monitoring  Enable firewall logging  Log denied packets and suspicious activity  No visibility of attacks firewall-cmd --set-log-denied=all 9. Service Hardening  Remove unnecessary services  Disable unused ports/services  Increased attack surface firewall-cmd --permanent --remove-service=ftp firewall-cmd –reload 10. (Situational – Oracle RAC) Application-Specific Security  Ensure firewall does not disrupt cluster communicationFirewall rules must allow RAC interconnect and DB ports  Cluster failure, node eviction  Validate with DBA Allow required ports firewall-cmd --permanent --add-port=1521/tcp firewall-cmd --permanent --add-port=1630-1640/tcp firewall-cmd –reload 11. (Situational – Oracle RAC Alternative) Exception Handling  Disable host firewall if required for RAC stability  In RAC, host firewall may be disabled  Misconfiguration leads to outage systemctl stop firewalld systemctl disable firewalld systemctl status firewalld Strong network firewall (FAR), VLAN isolation, IDS/IPSNote:  • Apply host-based firewalls or network firewalls with port filtering tools on end systems, with a default-deny rule that drops all traffic except those services and ports that are explicitly allowed as per business requirement (IDS/IPS, SIEM monitoring, and strict access control mechanisms) then control will be safe• Ensure any single firewall configuration utility is in use. FirewallD should be configured or enabled either with iptables or NFtables.
Modify Network Parameters	All required host-only parameters configured	"net.ipv4.conf.all.rp_filter = 1
net.ipv4.conf.all.send_redirects = 0
net.ipv4.conf.default.rp_filter = 1
net.ipv4.conf.default.send_redirects = 0
net.ipv4.icmp_ignore_bogus_error_responses = 1
net.ipv4.ip_forward = 1
net.ipv4.tcp_syncookies = 1"	Ensure system not acting as a router; enable protections	Disable forwarding, send_redirects; enable rp_filter and            tcp_syncookies.	Forwarding or redirects may allow unintended routing; lack of protections            increases risk.	Set via sysctl -w or conf files: ip_forward=0; send_redirects=0; rp_filter=1; tcp_syncookies=1; icmp_ignore_bogus_error_responses=1.
